import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MemberspasscodePage } from './memberspasscode';

@NgModule({
  declarations: [
   // MemberspasscodePage,
  ],
  imports: [
    IonicPageModule.forChild(MemberspasscodePage),
  ],
})
export class MemberspasscodePageModule {}
